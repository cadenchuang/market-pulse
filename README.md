# market-pulse
A high-throughput streaming pipeline that ingests live financial news, runs quantized FinBERT sentiment and entity extraction across a pool of worker processes, and computes a heuristic "sentiment shock" signal in real time.
